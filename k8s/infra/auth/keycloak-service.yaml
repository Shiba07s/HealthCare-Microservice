apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: healthcare-k8s
  labels:
    app: keycloak
    tier: auth
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: keycloak
    tier: auth