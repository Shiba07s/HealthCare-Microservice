# Define a 'Service' to expose MySQL
apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: healthcare-k8s
  labels:
    app: mysql
    tier: database
spec:
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    app: mysql
    tier: database
  clusterIP: None